//#include <caas/optimization/simplex.hpp>

#include <iostream>

int main()
{
    //using Point = caas::Point< 1 >;

    //auto const f3{ []( Point const & x ){ return std::pow( x[ 0 ] - 3, 2 ); } };

    //double x0, distance, precision, alpha, beta, gamma, sigma;
    //std::cin >> x0 >> distance >> precision >> alpha >> beta >> gamma >> sigma;

    //auto xm{ caas::optimization::simplex( f3, Point{ x0 }, distance, precision, alpha, beta, gamma, sigma ) };
    //std::cout << "Solution: f3(" << xm << ") = " << f3( xm ) << '\n';

    return 0;
}
